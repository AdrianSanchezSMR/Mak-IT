# 1. Conexión a MySQL
# Quitamos el SSL para evitar problemas de permisos
spring.datasource.url=jdbc:mysql://localhost:3307/make_it_db?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# 2. Hibernate (Actualizado para Spring Boot 4 / Hibernate 7)
# Cambiamos MySQL8Dialect por uno más genérico que no da error
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 3. Puerto
server.port=8080